---
title: Research RAG Codebase Knowledge Graph
description: Interactive system architecture and module relationship graph for the Research RAG system.
tags:
  - root
  - architecture
  - graphify
---

# Research RAG System — Codebase Knowledge Graph

Welcome to the interactive knowledge graph for the **Research RAG (`research-system-rag`)** project. This documentation and relationship map was generated by extracting structural dependencies and semantic concepts across **3,087 markdown notes** (`3,252 nodes`, `8,296 edges`).

---

## 🔍 How to Explore This Vault

* **Interactive Graph View**: Click the **Graph View** icon on the top right (desktop) or menu bar to explore the force-directed cluster graph. Click any node to navigate directly to that component's rationale or implementation note.
* **Instant Search**: Press `Ctrl+K` (or `Cmd+K` on macOS) or click the **Search bar** above to instantly search across all `3,087` notes using full-text `FlexSearch`.
* **Local Neighborhoods**: Every individual note page displays a localized dependency graph showing its immediate callers, callees, and design references.

---

## 🧭 Primary Entry Points & Core Documentation

To begin navigating the codebase architecture, start exploring from these core nodes:

* [[GRAPHIFY]] — Authoritative guide on knowledge graph extraction, build commands, and corpus filtering.
* [[CONVENTIONS]] — System-wide architectural invariants, error handling rules, and retry conventions.
* [[README]] — Core RAG system overview, component architecture, and runtime requirements.

---

## 📊 Quantitative Metrics Summary

| Metric | Verified Count |
| :--- | :--- |
| **Total Analyzed Notes** | `3,087 files` |
| **Graph Nodes** | `3,252 entities` |
| **Graph Edges** | `8,296 relationships` |
| **Detected Communities** | `182 semantic clusters` |
| **Engine** | `Quartz v4.0.8 + D3 Force Layout` |

---

## ⚙️ D3 Graph Physics & Readability Calibration

Because a complete codebase extraction generates over `3,250 nodes` (`test_*.md` unit tests, `__init__.py` modules, `rationale_*.md` notes), the D3 force layout in this repository (`quartz.layout.ts`) has been calibrated to prevent dense, overlapping "hairball" clusters:

* **Triple Node Repulsion (`repelForce: 1.5`)**: Increased from the default `0.5` to strongly repel neighboring nodes so text labels have breathing room.
* **Double Edge Distance (`linkDistance: 60`)**: Increased from `30` to `60` so distinct semantic clusters spread out clearly across the global canvas.
* **Weaker Center Attraction (`centerForce: 0.2`)**: Reduced from `0.3` to allow clusters to drift outward into distinct communities rather than collapsing into a tight ball at the center.
* **Local Neighborhood Precision (`localGraph: { depth: 1, linkDistance: 40 }`)**: When viewing any individual note page, the sidebar graph isolates only the immediate `depth: 1` callers/callees (~10 to 25 nodes), ensuring crisp and 100% human-readable daily navigation.
